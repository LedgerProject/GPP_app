// React import
import React from 'react';

// Intro screen import
import ContentView from '../views/intro';

export const IntroScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
