// React import
import React from 'react';

// Sign-in screen import
import ContentView from '../views/sign-in';

export const SignInScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
