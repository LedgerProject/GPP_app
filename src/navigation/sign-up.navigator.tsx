// React import
import React from 'react';

// Sign-up screen import
import ContentView from '../views/sign-up';

export const SignUpScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
