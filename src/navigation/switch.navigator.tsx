// React import
import React from 'react';

// Switch screen import
import ContentView from '../views/switch';

export const SwitchScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
