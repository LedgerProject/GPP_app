// React import
import React from 'react';

// Forgot password screen import
import ContentView from '../views/forgot-password';

export const ForgotPassword = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
